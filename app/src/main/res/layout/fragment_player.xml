<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/playerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="12dp"
    android:paddingBottom="8dp">

    <!-- 1. Header Bar: Chevron Down (<), Centered Title with 3 Interactive Dots, Equalizer -->
    <RelativeLayout
        android:id="@+id/headerBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="4dp"
        android:layout_alignParentTop="true">

        <ImageButton
            android:id="@+id/btnCollapse"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_chevron_down"
            android:contentDescription="Minimize to Library" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:orientation="vertical"
            android:gravity="center">

            <TextView
                android:id="@+id/tvPlayerHeaderTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="NOW PLAYING"
                android:textSize="11sp"
                android:letterSpacing="0.15"
                android:textStyle="bold"
                android:textColor="#E6FFFFFF" />

            <!-- 3 Interactive Dots Indicator under Header Title: Queue | Player | Lyrics -->
            <LinearLayout
                android:id="@+id/llHeaderDots"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginTop="5dp">

                <!-- Left Dot: Queue -->
                <FrameLayout
                    android:id="@+id/flDotQueue"
                    android:layout_width="24dp"
                    android:layout_height="20dp"
                    android:clickable="true"
                    android:focusable="true">

                    <View
                        android:id="@+id/dotQueue"
                        android:layout_width="6dp"
                        android:layout_height="6dp"
                        android:layout_gravity="center"
                        android:background="@drawable/tab_indicator_unselected" />
                </FrameLayout>

                <!-- Center Dot: Now Playing Player -->
                <FrameLayout
                    android:id="@+id/flDotPlayer"
                    android:layout_width="24dp"
                    android:layout_height="20dp"
                    android:clickable="true"
                    android:focusable="true">

                    <View
                        android:id="@+id/dotPlayer"
                        android:layout_width="8dp"
                        android:layout_height="8dp"
                        android:layout_gravity="center"
                        android:background="@drawable/tab_indicator_selected" />
                </FrameLayout>

                <!-- Right Dot: Lyrics -->
                <FrameLayout
                    android:id="@+id/flDotLyrics"
                    android:layout_width="24dp"
                    android:layout_height="20dp"
                    android:clickable="true"
                    android:focusable="true">

                    <View
                        android:id="@+id/dotLyrics"
                        android:layout_width="6dp"
                        android:layout_height="6dp"
                        android:layout_gravity="center"
                        android:background="@drawable/tab_indicator_unselected" />
                </FrameLayout>
            </LinearLayout>
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnEqualizer"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_equalizer"
            android:contentDescription="Equalizer" />
    </RelativeLayout>

    <!-- 2. Sub-Pages ViewPager2: Page 0 (Queue) | Page 1 (Now Playing) | Page 2 (Lyrics) -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/vpPlayerContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/headerBar" />

</RelativeLayout>
